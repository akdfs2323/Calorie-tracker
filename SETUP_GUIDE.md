# 🚀 คู่มือการติดตั้ง CalorieMate

## 📋 ขั้นตอนการติดตั้งทีละขั้น

### 1️⃣ ติดตั้ง Dependencies

```bash
npm install
```

หรือถ้าใช้ yarn:
```bash
yarn install
```

### 2️⃣ ตั้งค่า Supabase Database

#### 2.1 สร้างโปรเจค Supabase
1. ไปที่ https://supabase.com/
2. คลิก "Start your project"
3. สร้างโปรเจคใหม่ (เลือก region ที่ใกล้ที่สุด เช่น Singapore)
4. รอให้โปรเจคถูกสร้าง (ประมาณ 2-3 นาที)

#### 2.2 รัน Database Schema
1. ไปที่ SQL Editor ในเมนูด้านซ้าย
2. คลิก "New query"
3. คัดลอกโค้ดทั้งหมดจากไฟล์ `supabase-schema.sql`
4. วางลงใน SQL Editor
5. คลิก "Run" หรือกด `Ctrl + Enter`
6. ตรวจสอบว่าทุก table ถูกสร้างสำเร็จ

#### 2.3 เปิดใช้งาน Email Authentication
1. ไปที่ Authentication > Providers
2. เปิดใช้งาน "Email"
3. ตั้งค่า Confirm email เป็น false (เพื่อให้ทดสอบง่าย)

#### 2.4 คัดลอก API Keys
1. ไปที่ Settings > API
2. คัดลอก:
   - `Project URL` → จะนำไปใส่ใน `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public` key → จะนำไปใส่ใน `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### 3️⃣ ตั้งค่า Google Gemini API

#### 3.1 สร้าง API Key
1. ไปที่ https://makersuite.google.com/app/apikey
2. เข้าสู่ระบบด้วย Google Account
3. คลิก "Create API Key"
4. เลือก "Create API key in new project" หรือใช้โปรเจคที่มีอยู่
5. คัดลอก API Key ที่ได้

#### 3.2 ข้อควรรู้เกี่ยวกับ Gemini API
- **ฟรี**: ใช้งานได้ฟรี 60 requests/minute
- **รองรับภาพ**: วิเคราะห์รูปภาพได้
- **รองรับภาษาไทย**: ตอบคำถามภาษาไทยได้ดี

### 4️⃣ ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local` ที่ root directory:

```env
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Gemini API Configuration
GEMINI_API_KEY=AIzaSy...
```

**⚠️ สำคัญ**: อย่า commit ไฟล์ `.env.local` ขึ้น git!

### 5️⃣ รันโปรเจค

```bash
npm run dev
```

เปิดเบราว์เซอร์ไปที่: http://localhost:3000

### 6️⃣ ทดสอบระบบ

1. **สมัครสมาชิก**: ไปที่หน้า Signup
2. **เข้าสู่ระบบ**: Login ด้วย email/password ที่สมัคร
3. **ตั้งค่าโปรไฟล์**: กรอกน้ำหนัก ส่วนสูง เป้าหมาย
4. **ถ่ายรูปอาหาร**: ทดสอบ AI วิเคราะห์อาหาร
5. **แชทกับ AI**: ทดสอบระบบแชทบอท

## 🐛 Troubleshooting

### ปัญหา: "Supabase client has not been initialized"
**แก้ไข**: ตรวจสอบว่า environment variables ถูกต้อง และ restart dev server

### ปัญหา: "API key not valid"
**แก้ไข**: ตรวจสอบว่า Gemini API key ถูกต้อง และยังไม่หมดอายุ

### ปัญหา: "Failed to fetch"
**แก้ไข**: ตรวจสอบว่า Supabase RLS policies ถูกต้อง (รัน schema.sql ใหม่)

### ปัญหา: ภาพอาหารวิเคราะห์ไม่ได้
**แก้ไข**: 
- ตรวจสอบว่าภาพมีขนาดไม่เกิน 4MB
- ลองใช้ภาพที่คมชัดและมีแสงดี
- ตรวจสอบ Gemini API quota

## 📱 Features Checklist

- ✅ Landing Page
- ✅ Login/Signup/Logout
- ✅ Dashboard หน้าหลัก
- ✅ Profile & Goal Setting
- ✅ Camera - Food Analysis with AI
- ✅ Chat - AI Nutrition Advisor
- ✅ Daily Calorie Tracking
- ✅ Nutrition Summary (Protein/Carbs/Fat)
- ✅ Responsive Design
- ✅ Beautiful UI with Glassmorphism

## 🎨 การปรับแต่ง UI

### เปลี่ยนสีธีม
แก้ไขไฟล์ `tailwind.config.js`:
```js
colors: {
  primary: { ... },  // เปลี่ยนสีหลัก
  accent: { ... },   // เปลี่ยนสีเสริม
}
```

### เปลี่ยนฟอนต์
แก้ไขไฟล์ `app/layout.tsx`:
```ts
const poppins = Poppins({ ... });
const dmSans = DM_Sans({ ... });
```

## 🚀 Deploy to Production

### Vercel (แนะนำ)
1. Push โค้ดขึ้น GitHub
2. ไปที่ https://vercel.com/
3. Import Repository
4. ตั้งค่า Environment Variables
5. Deploy!

### Environment Variables ที่ต้องตั้งค่าใน Vercel:
```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
GEMINI_API_KEY
```

## 📊 Database Schema Overview

### Tables:
1. **profiles** - ข้อมูลโปรไฟล์ผู้ใช้
2. **calorie_entries** - บันทึกแคลอรี่รายวัน
3. **chat_messages** - ประวัติการแชท

### Security:
- Row Level Security (RLS) enabled
- Users can only access their own data
- Automatic user_id validation

## 🔐 Security Best Practices

1. ✅ Never commit `.env.local`
2. ✅ Use environment variables for sensitive data
3. ✅ Enable RLS on all tables
4. ✅ Validate user input on both client and server
5. ✅ Use HTTPS in production

## 💡 Tips & Tricks

### เพิ่มประสิทธิภาพ AI
- ถ่ายรูปอาหารให้ชัดเจน
- ใช้แสงที่ดี
- ถ่ายให้เห็นอาหารทั้งหมด

### การใช้งาน Chatbot
- ถามคำถามที่ชัดเจน
- ให้บริบทเพิ่มเติม
- สามารถถามเป็นภาษาไทยได้

### จัดการข้อมูล
- ลบบันทึกเก่าเป็นประจำ
- Export ข้อมูลจาก Supabase ได้
- Backup database เป็นประจำ

## 📞 Support

มีปัญหาหรือข้อสงสัย? 
- เปิด Issue บน GitHub
- ดูเอกสาร Supabase: https://supabase.com/docs
- ดูเอกสาร Gemini: https://ai.google.dev/docs

---

**Happy Coding! 🎉**
